# Phases

## Login Phase
After these steps (finally), we can now finally send our username/password
to the server to log in, so the client creates a Login Packet, puts the 
credentials in and sends it

The Authentication agent will check if the credentials match an
